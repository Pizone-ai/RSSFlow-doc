# 信息流模式 / Flow View

### 中文

信息流模式是一个全屏的大视野视图，类似于瀑布流，能够直观地将多篇文章按照日期或主题分类进行汇聚，方便您进行高效的阅读、比对和与 AI 进行交互。

**投射模式切换（分组方式）：**
- **日视图 (Dayflow)**：按照日期（如“今天”、“昨天”及具体的日期分组）对文章进行归档，并在每一天内按标签进行次级分类。包含专为追踪突发新闻设立的“最近 24 小时”特色分组。
- **标签视图 (Tagflow)**：以标签主题（如 AI、科技、商业）作为首要分组方式，方便您快速了解该主题下的所有最新资讯。

**卡片显示模式：**
- **标准模式**：显示文章的标题、序号以及 AI 总结的核心观点（以明显的引用栏样式突出显示），鼠标悬停会有上浮阴影反馈。
- **极简模式**：极度紧凑的列表，仅显示序号和单行截断的标题，适合快速浏览并定位文章。

**右侧导航锚点：**
- 列表右侧有一条极简的圆点状滑轨，滑动屏幕时会自动显现。
- 每个圆点代表一个日期分组或主题标签分组，点击圆点可以像目录一样瞬间跳转至对应的内容区域。

**浮动 AI 聊天：**
- 每个分组的右下角均有彩色的 AI 聊天按钮。点击后可以针对当前整个分组（例如“今日所有关于 AI 的文章”）向 AI 助手提问、进行综合剖析或进行观点探寻。

**底部新闻跑马灯（Ticker Bar）：**
- 屏幕底部提供一条流畅的新闻播报条，能将新生成的 AI 摘要转换为文本和语音自动在后台播放。
- 支持点击暂停、继续或跳过当前播放内容。
- 当您开辟了多个信息流页面时，系统会自动协调，保证只有一个网页正在进行声音播报，防止干扰。
- 如果不需要此播报，您可以在通用设置中随时关闭。

**自适应多列布局：**
- 视图会根据您当前的显示器宽度自动调节排版列数（单列、双列或多列）。在您手动拉伸浏览器窗口时，列表会保持当前状态并在调整完毕后自动平滑完成多列适配。

---

### English

Flow View is a full-screen, dashboard-style interface. It aggregates articles in a waterfall format sorted by date or tags, making it highly effective to analyze themes, review AI insights, and interact with the AI assistant.

**Projection Modes:**
- **Dayflow**: Groups articles by date (e.g., Today, Yesterday, specific dates), then categories. Includes a dynamic "Recent 24 Hours" group for hot topics.
- **Tagflow**: Categorizes articles by tags first, then groups them by date, helping you deep-dive into specific areas.

**Card Display Modes:**
- **Standard**: Shows index, title, and key AI opinions styled as a clean blockquote. Hovering creates a subtle hover-lift effect.
- **Mini**: Shows only index and single-line truncated titles to scan headlines rapidly.

**Right-side Navigation Rail:**
- A vertical dot indicator appears on the right edge of the screen when scrolling or hovering.
- Each dot acts as a quick bookmark link. Click to scroll immediately to that date or tag group.

**Grouped AI Chat:**
- A chat action button floats in the corner of each group. Clicking this launches a targeted AI session with the current group's articles preloaded as context.

**Bottom News Ticker (Ticker Bar):**
- A marquee banner displays rolling titles and reads AI summaries out loud automatically.
- Playback controls allow you to play, pause, or skip items easily.
- If multiple Flow pages are open, a built-in coordination system ensures only one page plays audio at a time.
- Can be disabled anytime under settings.

**Multi-column Auto-adaptivity:**
- The page dynamically adjusts the column count based on your screen width. Dragging or resizing the window keeps the columns clean and automatically snaps to grid.
