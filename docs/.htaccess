# 语言重定向规则
RewriteEngine On
RewriteBase /RSSFlow-doc/

# 如果是根目录访问，检查 Accept-Language 头
RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^zh [NC]
RewriteRule ^$ /zh/ [R=302,L]

# 默认重定向到英文版
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /en/ [R=302,L] 